export const dsIntermediate = [
`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(a + 5)`,

`Explain bias–variance tradeoff with a practical example.`,

`What will be the output?
import pandas as pd
df = pd.DataFrame({'A':[1,2,3,4]})
print(df['A'].median())`,

`You trained a model and training accuracy = 99%, validation accuracy = 70%. What is happening and what will you do?`,

`Calculate precision if TP = 30 and FP = 20.`,

`What will be the output?
x = [1,2,3]
print(x + x)`,

`Explain the difference between L1 and L2 regularization mathematically.`,

`What happens if learning rate is extremely high in gradient descent?`,

`What will be the output?
import numpy as np
a = np.array([[1,2],[3,4]])
print(a.shape)`,

`In which situation is F1-score better than accuracy?`,

`What is multicollinearity and how can you detect it?`,

`What will be the output?
import pandas as pd
df = pd.DataFrame({'A':[1,2,None]})
print(df['A'].isnull().sum())`,

`If correlation between two variables is 0.92, what issue can arise?`,

`What will be the output?
import numpy as np
a = np.array([10,20,30])
print(np.mean(a))`,

`How does Random Forest reduce overfitting?`,

`What happens if K in KNN is set very large?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
b = a
b[0] = 100
print(a)`,

`Suppose dataset is highly imbalanced (95% class 0). Which metric will you use and why?`,

`What is the curse of dimensionality?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(a > 1)`,

`What assumptions does linear regression make?`,

`What happens if you increase regularization strength too much?`,

`What will be the output?
import pandas as pd
df = pd.DataFrame({'A':[1,2,3]})
df['B'] = df['A'] * 2
print(df)`,

`Explain entropy and information gain in decision trees.`,

`If p-value > 0.05, what does it indicate?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(a / 2)`,

`How does k-means initialize cluster centers?`,

`What happens if K equals number of data points in k-means?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(np.std(a))`,

`Your deployed model performance drops after 3 months. What might be happening?`
];