export const dsFinal = [
`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(a * 3)`,

`Explain bias–variance tradeoff with a practical example.`,

`What will be the output?
x = [1,2,3]
print(x + x)`,

`If training accuracy is 95% and validation accuracy is 60%, what is happening?`,

`TP=50, FP=10, FN=20, TN=20
Calculate precision and recall.`,

`What will be the output?
import numpy as np
a = np.array([[1,2],[3,4]])
print(a.shape)`,

`What assumptions does linear regression make?`,

`What is the difference between L1 and L2 regularization?`,

`What will be the output?
import pandas as pd
df = pd.DataFrame({'A':[1,2,None]})
print(df['A'].isnull().sum())`,

`When should you use F1-score instead of accuracy?`,

`What is multicollinearity and how does it affect regression?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
b = a
b[0] = 100
print(a)`,

`Explain how Random Forest reduces overfitting.`,

`What happens if K in KNN is very small? What if very large?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(np.mean(a))`,

`What is data leakage? Give an example.`,

`What is the curse of dimensionality?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(a > 2)`,

`How does gradient descent update weights?`,

`What happens if learning rate is too high?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(np.dot(a,a))`,

`Explain entropy and information gain.`,

`If two models have similar accuracy, how do you choose between them?`,

`What will be the output?
import pandas as pd
df = pd.DataFrame({'A':[1,2,3]})
df['B'] = df['A'] * 2
print(df)`,

`What is concept drift?`,

`If your production model performance drops over time, how will you investigate?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(np.std(a))`,

`Explain the difference between generative and discriminative models.`,

`How would you handle severe class imbalance in a fraud detection system?`,

`Design a recommendation system at high level. What components are required?`
];