export const dsBasic = [
`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(a * 2)`,

`What is the difference between supervised and unsupervised learning?`,

`What will be the output?
x = [1,2,3]
print(x * 2)`,

`What is overfitting?`,

`What will be the output?
import pandas as pd
df = pd.DataFrame({'A':[1,2,3]})
print(df['A'].mean())`,

`What is the difference between classification and regression?`,

`If TP = 40, TN = 50, FP = 5, FN = 5 — what is accuracy?`,

`What is a confusion matrix?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(np.mean(a))`,

`What is precision?`,

`What will be the output?
import pandas as pd
df = pd.DataFrame({'A':[1,None,3]})
print(df['A'].isnull().sum())`,

`What is recall?`,

`What is the difference between normalization and standardization?`,

`What will be the output?
import numpy as np
a = np.array([10,20,30])
print(a + 5)`,

`What is an outlier?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(a > 1)`,

`What is cross-validation?`,

`If dataset is highly imbalanced, why is accuracy misleading?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(np.std(a))`,

`What is linear regression?`,

`What is logistic regression used for?`,

`What will be the output?
import pandas as pd
df = pd.DataFrame({'A':[1,2,3]})
df['B'] = df['A'] * 2
print(df)`,

`What is gradient descent?`,

`What is feature engineering?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
b = a
b[0] = 100
print(a)`,

`What is PCA?`,

`What is k-means clustering?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(a / 2)`,

`What are commonly used Python libraries in data science?`,

`You are given raw messy data. What are the first three things you will do?`
];