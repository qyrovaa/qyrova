export const dsHardcore = [
`What will be the output?
import numpy as np
a = np.array([1,2,3])
b = a
b = b + 5
print(a)`,

`Explain mathematically how L2 regularization modifies the cost function in linear regression.`,

`What will be the output?
import numpy as np
a = np.array([[1,2],[3,4]])
print(a.T)`,

`If training loss decreases but validation loss increases, what is happening? Explain in terms of bias and variance.`,

`Given: TP=45, FP=5, FN=15, TN=35
Calculate precision, recall, and F1-score.`,

`What will be the output?
import pandas as pd
df = pd.DataFrame({'A':[1,2,3]})
df.loc[1,'A'] = 100
print(df)`,

`Derive the gradient update rule for linear regression using gradient descent.`,

`What happens if features are not scaled in gradient descent?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(np.var(a))`,

`Explain the curse of dimensionality mathematically.`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(a[[True, False, True]])`,

`What assumptions does logistic regression make?`,

`If two features are perfectly correlated, what happens to the regression coefficients?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(a.reshape(3,1).shape)`,

`Explain how Random Forest reduces variance.`,

`What is the difference between bagging and boosting mathematically?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(np.dot(a,a))`,

`If K in K-means equals number of samples, what happens to within-cluster variance?`,

`Explain entropy formula and why log is used.`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(a / 0)`,

`What is the difference between generative and discriminative models with equations?`,

`Why does high regularization increase bias?`,

`What will be the output?
import pandas as pd
df = pd.DataFrame({'A':[1,2,3]})
print(df.iloc[5])`,

`Explain gradient boosting step-by-step mathematically.`,

`If p-value is very small but effect size is tiny, what does that imply?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
b = a.copy()
b[0] = 100
print(a)`,

`Explain vanishing gradient problem.`,

`If learning rate is too small and epochs are limited, what happens?`,

`What will be the output?
import numpy as np
a = np.array([1,2,3])
print(np.corrcoef(a,a))`,

`Your production model performance drops gradually over months. Explain possible reasons in terms of data drift and concept drift.`
];